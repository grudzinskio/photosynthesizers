/**
 * Simple script to run validation and display results
 * This can be imported and executed to validate translations
 */

import { runValidation } from './validateTranslations';

// Run validation when this file is executed
runValidation();
